[package]
name = "cabal-store"
version = "0.1.0"
edition = "2021"
description = "Crash-safe typed persistence. Paths are injected, never discovered."
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
