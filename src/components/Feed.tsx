import React from "react";

/**
 * Retained as an extension point for a future event-backed activity feed.
 * The old version rendered fabricated users, prices, and task counts; an MVP
 * must never present those values as live mesh activity.
 */
export const Feed: React.FC = () => null;
